{
  "name": "nepal-election-webapp",
  "version": "1.0.0",
  "description": "Nepal Election Web Application",
  "scripts": {
    "dev": "concurrently \"cd backend && python main.py\" \"cd frontend && npm start\"",
    "install-deps": "cd frontend && npm install && cd ../backend && pip install -r requirements.txt"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
